"""Ошибки, о которых знают сразу несколько слоёв.

Размерность вектора это договор между моделью и колонкой в базе, и нарушить
его может любая из сторон: модель вернёт длину не ту, что объявлена, или
индекс окажется собран под прошлый бэкенд. Тип ошибки один, потому что и
причина одна, и лечение одно.
"""

from __future__ import annotations


class DimensionMismatch(RuntimeError):
    """Длина вектора не совпадает с той, под которую заведён индекс."""
